groups:
  - name: DataLight
    rules:
      - alert: ResourceManagerDown
        expr: process_start_time_seconds{job="YARN-ResourceManager"} == 0
        for: 10s
        labels:
          severity: critical
          hostname: |
            {{- $instance := $labels.instance }}
            {{- $instance | regexReplaceAll "^(.*?)(\\..*)?$" "$1" }}
        annotations:
          summary: "{{ $labels.job }} 进程在 {{ $labels.hostname }} 宕机"
          description: "{{ $labels.job }} 进程在 {{ $labels.hostname }} 已经宕机超过5分钟"