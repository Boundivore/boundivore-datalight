groups:
  - name: DataLight
    rules:
      - alert: JournalNodeDown
        expr: up{job="HDFS-JournalNode"} == 0
        for: 10s
        labels:
          severity: warning
        annotations:
          summary: "STATIC-{{ $labels.instance }}@{{ $labels.job }}"
          description: "{{ $labels.job }} 进程在 {{ $labels.instance }} 已经宕机"